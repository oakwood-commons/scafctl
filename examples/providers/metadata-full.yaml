# Run: scafctl run solution -f examples/providers/metadata-full.yaml
# Run: scafctl run resolver -f examples/providers/metadata-full.yaml

apiVersion: scafctl.io/v1
kind: Solution

metadata:
  name: metadata-full-example
  version: 1.0.0
  description: |
    Runtime metadata about scafctl and the current solution. Returns
    version info, CLI args, cwd, entrypoint, command path, and solution metadata.

spec:
  resolvers:
    runtime-meta:
      description: Full runtime metadata (version, args, cwd, entrypoint, solution)
      type: any
      resolve:
        with:
          - provider: metadata
