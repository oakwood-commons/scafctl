spec:
  workflow:
    actions:
      deploy:
        provider: message
        inputs:
          message:
            expr: "'Deploying ' + _.app_name + ' to ' + _.environment"
          type: success
