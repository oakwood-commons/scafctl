apiVersion: scafctl.io/v1
kind: Solution
metadata:
  name: circular-b
  version: 1.0.0
spec:
  resolvers:
    data:
      type: any
      resolve:
        with:
          - provider: solution
            inputs:
              source: "circular-a.yaml"
